name: Sync Wiki to Chirpy Blog

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 * * * *'  # 매시간 실행
  workflow_dispatch:  # 수동 실행 가능

jobs:
  wiki-to-blog:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout blog repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Clone Wiki repository
      run: |
        echo "=== Cloning Wiki repository ==="
        git clone https://github.com/${{ github.repository }}.wiki.git wiki-temp
        
        if [ -d "wiki-temp" ]; then
          echo "✓ Wiki repository cloned successfully"
          echo ""
          echo "Wiki files found:"
          ls -la wiki-temp/*.md 2>/dev/null || echo "No markdown files found"
        else
          echo "✗ Failed to clone wiki repository"
          echo "Please check if Wiki is enabled in repository settings"
          exit 1
        fi

    - name: Convert Wiki to Chirpy posts
      run: |
        echo ""
        echo "╔════════════════════════════════════════╗"
        echo "║   Wiki to Chirpy Blog Conversion      ║"
        echo "╚════════════════════════════════════════╝"
        echo ""
        
        # 디렉토리 생성
        mkdir -p _posts _drafts
        
        # 타임존 설정
        NOW_TZ="+0900"
        
        # 통계 변수
        CONVERTED_COUNT=0
        DRAFT_COUNT=0
        SKIPPED_COUNT=0
        ERROR_COUNT=0
        
        # Wiki 파일 처리
        for file in wiki-temp/*.md; do
          # 파일 존재 확인
          [ -f "$file" ] || continue
          
          FILENAME=$(basename "$file")
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📄 Processing: $FILENAME"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          # 메타데이터에서 제목 추출 시도
          META_TITLE=$(grep "^@title:" "$file" | cut -d':' -f2- | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
          
          if [ -n "$META_TITLE" ]; then
            TITLE="$META_TITLE"
            echo "📌 Title (from metadata): $TITLE"
          else
            # 첫 번째 # 헤더에서 제목 추출 (링크 제거)
            TITLE=$(grep -m 1 "^# " "$file" | \
              sed 's/^# //' | \
              sed 's/\[#\]([^)]*)//g' | \
              sed 's/\[\[.*\]\]//g' | \
              sed 's/\[.*\]([^)]*)//g' | \
              sed 's/  */ /g' | \
              sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            
            if [ -z "$TITLE" ]; then
              TITLE=$(basename "$file" .md)
              echo "⚠️  No title found, using filename: $TITLE"
            else
              echo "📌 Title (extracted): $TITLE"
            fi
          fi
          
          # 공개 여부 확인
          PUBLISH=$(grep "^@publish:" "$file" | awk '{print $2}' | tr -d '[:space:]')
          if [ "$PUBLISH" = "false" ]; then
            echo "⊘  Skipped (publish: false)"
            echo ""
            SKIPPED_COUNT=$((SKIPPED_COUNT + 1))
            continue
          fi
          
          # Slug 처리
          SLUG=$(grep "^@slug:" "$file" | awk '{print $2}' | tr -d '[:space:]')
          if [ -z "$SLUG" ]; then
            # 제목에서 slug 자동 생성
            SLUG=$(echo "$TITLE" | \
              iconv -t ascii//TRANSLIT 2>/dev/null || echo "$TITLE" | \
              tr '[:upper:]' '[:lower:]' | \
              sed 's/[^a-z0-9가-힣]/-/g' | \
              sed 's/--*/-/g' | \
              sed 's/^-//;s/-$//')
            echo "🔗 Slug (generated): $SLUG"
          else
            echo "🔗 Slug (custom): $SLUG"
          fi
          
          # 날짜 처리
          WIKI_DATE=$(grep "^@date:" "$file" | awk '{print $2}')
          if [ -z "$WIKI_DATE" ]; then
            POST_DATE=$(date +"%Y-%m-%d")
            POST_DATETIME=$(date +"%Y-%m-%d %H:%M:%S $NOW_TZ")
            echo "📅 Date (current): $POST_DATE"
          else
            POST_DATE="$WIKI_DATE"
            POST_DATETIME="$WIKI_DATE 09:00:00 $NOW_TZ"
            echo "📅 Date (from wiki): $POST_DATE"
          fi
          
          # 카테고리 추출
          CATEGORIES=$(grep "^@categories:" "$file" | cut -d':' -f2 | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | sed 's/,/, /g')
          if [ -z "$CATEGORIES" ]; then
            CATEGORIES="Wiki"
            echo "📁 Categories (default): $CATEGORIES"
          else
            echo "📁 Categories: $CATEGORIES"
          fi
          
          # 태그 추출
          TAGS=$(grep "^@tags:" "$file" | cut -d':' -f2 | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | sed 's/,/, /g')
          if [ -n "$TAGS" ]; then
            echo "🏷️  Tags: $TAGS"
          fi
          
          # Draft 여부 확인
          DRAFT=$(grep "^@draft:" "$file" | awk '{print $2}' | tr -d '[:space:]')
          
          # 파일 경로 결정
          if [ "$DRAFT" = "true" ]; then
            TARGET="_drafts/${SLUG}.md"
            echo "📝 Type: Draft"
            echo "💾 Output: $TARGET"
            DRAFT_COUNT=$((DRAFT_COUNT + 1))
          else
            TARGET="_posts/${POST_DATE}-${SLUG}.md"
            echo "📝 Type: Post"
            echo "💾 Output: $TARGET"
            CONVERTED_COUNT=$((CONVERTED_COUNT + 1))
          fi
          
          # Front Matter 생성
          {
            echo "---"
            echo "title: \"$TITLE\""
            echo "date: $POST_DATETIME"
            echo "categories: [$CATEGORIES]"
            if [ -n "$TAGS" ]; then
              echo "tags: [$TAGS]"
            fi
            echo "---"
            echo ""
          } > "$TARGET"
          
          # 본문 내용 추가 (메타데이터 주석과 메타태그 제외)
          # 첫 번째 # 제목도 제거 (Front Matter의 title로 대체됨)
          sed '/^<!--/,/^-->/d;/^@/d;1,/^# /d' "$file" >> "$TARGET"
          
          echo "✅ Converted successfully"
          echo ""
        done
        
        # 임시 폴더 삭제
        rm -rf wiki-temp
        
        # 변환 결과 요약
        echo ""
        echo "╔════════════════════════════════════════╗"
        echo "║        Conversion Summary              ║"
        echo "╚════════════════════════════════════════╝"
        echo ""
        echo "  📊 Statistics:"
        echo "  ─────────────────────────────────────"
        echo "  ✅ Posts created    : $CONVERTED_COUNT"
        echo "  📝 Drafts created   : $DRAFT_COUNT"
        echo "  ⊘  Files skipped    : $SKIPPED_COUNT"
        echo "  ❌ Errors           : $ERROR_COUNT"
        echo "  ─────────────────────────────────────"
        echo "  📈 Total processed  : $((CONVERTED_COUNT + DRAFT_COUNT + SKIPPED_COUNT + ERROR_COUNT))"
        echo ""

    - name: Commit and push changes
      run: |
        echo "╔════════════════════════════════════════╗"
        echo "║         Git Operations                 ║"
        echo "╚════════════════════════════════════════╝"
        echo ""
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        
        # 파일 추가
        git add _posts/ _drafts/
        
        # 상태 확인
        echo "📋 Git Status:"
        echo "─────────────────────────────────────"
        git status --short
        echo ""
        
        # 변경사항이 있는지 확인
        if git diff --staged --quiet; then
          echo "✓ No changes to commit"
          echo "  Wiki content is already up to date"
          echo ""
        else
          # 변경된 파일 목록
          echo "📝 Changed Files:"
          echo "─────────────────────────────────────"
          git diff --staged --name-status
          echo ""
          
          # 커밋 메시지 생성
          COMMIT_MSG="auto: sync wiki to blog [$(date +'%Y-%m-%d %H:%M:%S')]"
          
          echo "💾 Committing changes..."
          git commit -m "$COMMIT_MSG"
          
          echo "🚀 Pushing to repository..."
          git push origin main
          
          echo ""
          echo "✅ Wiki sync completed successfully!"
        fi

    - name: Workflow Summary
      if: always()
      run: |
        echo ""
        echo "╔════════════════════════════════════════╗"
        echo "║          Workflow Complete             ║"
        echo "╚════════════════════════════════════════╝"
        echo ""
        echo "  Repository  : ${{ github.repository }}"
        echo "  Workflow    : ${{ github.workflow }}"
        echo "  Run ID      : ${{ github.run_id }}"
        echo "  Triggered by: ${{ github.event_name }}"
        echo "  Status      : ${{ job.status }}"
        echo ""
        echo "  📅 Completed at: $(date +'%Y-%m-%d %H:%M:%S')"
        echo ""
