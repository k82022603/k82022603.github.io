---
title: "AI 코딩 도구 통합 개발자 매뉴얼 (2)"
date: 2026-01-04 08:00:00 +0900
categories: [AI,  Vibe Coding]
mermaid: [True]
tags: [AI,  GitHub,  Codex,  claude-code,  google-antigravity,  vibe-coding,  coding-agents,  Developer,  Claude.write]
---


> **Claude Code, Codex, Google Antigravity 완전 정복**  
> Git 워크플로우부터 PR 자동화까지, 실전에서 바로 쓰는 완벽 가이드

## 목차

- [[Part 2: Git 워크플로우 마스터](https://claude.ai/chat/d5ec2529-0920-484b-899b-fed022a4046c#part-2-git-%EC%9B%8C%ED%81%AC%ED%94%8C%EB%A1%9C%EC%9A%B0-%EB%A7%88%EC%8A%A4%ED%84%B0)](#part-2-git-워크플로우-마스터)
- [[Part 3: 도구별 완전 활용법](https://claude.ai/chat/d5ec2529-0920-484b-899b-fed022a4046c#part-3-%EB%8F%84%EA%B5%AC%EB%B3%84-%EC%99%84%EC%A0%84-%ED%99%9C%EC%9A%A9%EB%B2%95)](#part-3-도구별-완전-활용법)
- [[실전 예시](https://claude.ai/chat/d5ec2529-0920-484b-899b-fed022a4046c#%EC%8B%A4%EC%A0%84-%EC%98%88%EC%8B%9C)](#실전-예시)

---

## Part 2: Git 워크플로우 마스터

### 2.3 /pr: Pull Request 자동 생성

**파일 위치**: `~/.claude/commands/pr.md`

#### 설치 방법

```bash
# 명령어 디렉토리 생성
mkdir -p ~/.claude/commands

# 스크립트 파일 생성 (아래 스크립트 참조)
cat > ~/.claude/commands/pr.md << 'SCRIPT_END'
#!/bin/bash
set -e

# ... (스크립트 내용)
SCRIPT_END

# 실행 권한 부여
chmod +x ~/.claude/commands/pr.md
```

#### 전체 스크립트

{% raw %}
```bash
#!/bin/bash
set -e

#━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 함수 정의
#━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

check_environment() {
    echo "🔍 환경 확인 중..."
    
    if ! command -v gh &> /dev/null; then
        echo "❌ GitHub CLI (gh)가 설치되지 않았습니다"
        echo "설치: https://cli.github.com"
        exit 1
    fi
    
    if ! gh auth status &> /dev/null; then
        echo "❌ GitHub 인증이 필요합니다"
        echo "실행: gh auth login"
        exit 1
    fi
    
    if ! git rev-parse --git-dir > /dev/null 2>&1; then
        echo "❌ Git 저장소가 아닙니다"
        exit 1
    fi
    
    echo "✅ 환경 확인 완료"
}

setup_branches() {
    echo ""
    echo "📂 브랜치 설정 중..."
    
    CURRENT_BRANCH=$(git branch --show-current)
    
    if [ -z "$CURRENT_BRANCH" ]; then
        echo "❌ 현재 브랜치를 확인할 수 없습니다"
        exit 1
    fi
    
    echo "   현재 브랜치: $CURRENT_BRANCH"
    
    if git show-ref --verify --quiet refs/heads/main; then
        DEFAULT_BASE="main"
    elif git show-ref --verify --quiet refs/heads/master; then
        DEFAULT_BASE="master"
    else
        DEFAULT_BASE="develop"
    fi
    
    echo "   기본 브랜치: $DEFAULT_BASE"
    
    echo ""
    echo "Base 브랜치를 선택하세요:"
    echo "  1) $DEFAULT_BASE (기본)"
    echo "  2) develop"
    echo "  3) 직접 입력"
    echo ""
    echo -n "선택 [1/2/3, 기본=1]: "
    read -r base_choice
    
    case "$base_choice" in
        2) BASE_BRANCH="develop" ;;
        3)
            echo -n "Base 브랜치 이름: "
            read -r BASE_BRANCH
            ;;
        *) BASE_BRANCH="$DEFAULT_BASE" ;;
    esac
    
    echo "   → Base: $BASE_BRANCH"
}

analyze_commits() {
    echo ""
    echo "📊 커밋 분석 중..."
    
    COMMITS=$(git log "$BASE_BRANCH".."$CURRENT_BRANCH" --oneline)
    COMMIT_COUNT=$(git rev-list --count "$BASE_BRANCH".."$CURRENT_BRANCH")
    
    if [ "$COMMIT_COUNT" -eq 0 ]; then
        echo "❌ 새 커밋이 없습니다"
        exit 1
    fi
    
    echo ""
    echo "포함될 커밋:"
    echo "─────────────────────────────────────"
    echo "$COMMITS"
    echo "─────────────────────────────────────"
    echo "커밋 수: $COMMIT_COUNT"
}

generate_pr_title() {
    echo ""
    echo "📝 PR 제목 생성 중..."
    
    if [ "$COMMIT_COUNT" -eq 1 ]; then
        PR_TITLE=$(git log -1 --pretty=%s)
    else
        FEAT_COMMIT=$(echo "$COMMITS" | grep "^[a-f0-9]* feat" | head -1 | sed 's/^[a-f0-9]* //')
        
        if [ -n "$FEAT_COMMIT" ]; then
            PR_TITLE="$FEAT_COMMIT"
        else
            PR_TITLE=$(echo "$COMMITS" | head -1 | sed 's/^[a-f0-9]* //')
        fi
    fi
    
    echo ""
    echo "제안된 PR 제목:"
    echo "  $PR_TITLE"
    echo ""
    echo -n "사용하시겠습니까? [y/n/수정]: "
    read -r title_answer
    
    case "$title_answer" in
        n|N|no|No)
            echo -n "새 제목 입력: "
            read -r PR_TITLE
            ;;
        수정|e|edit)
            echo -n "수정할 제목: "
            read -r new_title
            if [ -n "$new_title" ]; then
                PR_TITLE="$new_title"
            fi
            ;;
    esac
    
    echo "   최종 제목: $PR_TITLE"
}

load_pr_template() {
    if [ -f .github/PULL_REQUEST_TEMPLATE.md ]; then
        cat .github/PULL_REQUEST_TEMPLATE.md
    else
        cat <<'TEMPLATE_EOF'
## 변경사항 요약
[변경 내용을 한 줄로]

## 상세 설명

### 추가된 기능
- 

### 수정된 버그
- 

### 변경된 사항
- 

## 테스트 방법

### 로컬 테스트

# 재현 단계

### 예상 결과
- 

## 체크리스트

- [ ] 코드가 프로젝트 스타일 가이드를 따릅니다
- [ ] 자체 코드 리뷰를 수행했습니다
- [ ] 코드에 주석을 추가했습니다
- [ ] 문서를 업데이트했습니다
- [ ] 테스트를 추가/업데이트했습니다
- [ ] 모든 테스트가 통과합니다

## 관련 이슈

Closes #
Related to #
TEMPLATE_EOF
    fi
}

generate_pr_body_with_ai() {
    local template="$1"
    
    echo ""
    echo "🤖 AI가 PR 본문을 작성 중..."
    
    local changed_files
    changed_files=$(git diff --name-status "$BASE_BRANCH".."$CURRENT_BRANCH" 2>/dev/null || echo "(정보 없음)")
    
    local diff_stat
    diff_stat=$(git diff "$BASE_BRANCH".."$CURRENT_BRANCH" --stat 2>/dev/null || echo "(정보 없음)")
    
    if ! command -v claude &> /dev/null; then
        echo "⚠️  Claude CLI 없음. 템플릿 사용"
        echo "$template"
        return 1
    fi
    
    local pr_body
    pr_body=$(claude 2>/dev/null <<CLAUDE_PROMPT
다음 Git 변경사항으로 PR 본문을 작성해줘.

커밋 이력:
$COMMITS

변경된 파일:
$changed_files

변경 통계:
$diff_stat

템플릿:
$template

요구사항:
1. 템플릿의 모든 섹션 채우기
2. 구체적이고 명확하게
3. 마크다운 형식
4. 한국어 사용

출력은 순수 마크다운만 (프리앰블 없이)
CLAUDE_PROMPT
)
    
    if [ $? -ne 0 ] || [ -z "$pr_body" ]; then
        echo "⚠️  AI 생성 실패. 템플릿 사용"
        echo "$template"
        return 1
    fi
    
    echo "$pr_body"
    return 0
}

confirm_and_edit_pr_body() {
    local pr_body="$1"
    
    echo ""
    echo "─────────────────────────────────────"
    echo "$pr_body"
    echo "─────────────────────────────────────"
    echo ""
    echo "이 본문을 사용하시겠습니까?"
    echo "  y/yes    : 그대로 사용"
    echo "  e/edit   : 에디터로 수정"
    echo "  m/manual : 수동 입력"
    echo "  n/no     : 취소"
    echo ""
    echo -n "선택 [y/e/m/n]: "
    read -r answer
    
    answer=$(echo "$answer" | tr '[:upper:]' '[:lower:]' | tr -d ' ')
    
    case "$answer" in
        y|yes|"")
            echo "$pr_body"
            return 0
            ;;
        e|edit|수정)
            local temp_file
            temp_file=$(mktemp /tmp/pr_body.XXXXXX.md)
            echo "$pr_body" > "$temp_file"
            ${EDITOR:-vim} "$temp_file"
            pr_body=$(cat "$temp_file")
            rm -f "$temp_file"
            echo "$pr_body"
            return 0
            ;;
        m|manual)
            echo ""
            echo "PR 본문 입력 (Ctrl+D로 완료):"
            cat
            return 0
            ;;
        n|no|취소)
            echo "❌ PR 생성 취소"
            return 1
            ;;
        *)
            echo "❌ 잘못된 입력: $answer"
            confirm_and_edit_pr_body "$pr_body"
            return $?
            ;;
    esac
}

generate_pr_body() {
    echo ""
    echo "📄 PR 본문 생성 중..."
    
    local template
    template=$(load_pr_template)
    
    local pr_body
    pr_body=$(generate_pr_body_with_ai "$template")
    
    local final_pr_body
    final_pr_body=$(confirm_and_edit_pr_body "$pr_body")
    
    if [ $? -ne 0 ] || [ -z "$final_pr_body" ]; then
        echo "❌ PR 본문 생성 실패"
        exit 1
    fi
    
    FINAL_PR_BODY="$final_pr_body"
}

assign_reviewers() {
    echo ""
    echo "👥 리뷰어 지정 중..."
    
    REVIEWERS=""
    
    if [ -f .github/CODEOWNERS ]; then
        echo "   CODEOWNERS 파일 발견"
        
        local changed_files
        changed_files=$(git diff --name-only "$BASE_BRANCH".."$CURRENT_BRANCH")
        
        local owners=()
        while IFS= read -r file; do
            local owner
            owner=$(grep -E "^${file}" .github/CODEOWNERS | awk '{print $2}' | head -n 1)
            if [ -n "$owner" ]; then
                owner=$(echo "$owner" | sed 's/@//')
                owners+=("$owner")
            fi
        done <<< "$changed_files"
        
        if [ ${#owners[@]} -gt 0 ]; then
            REVIEWERS=$(printf '%s\n' "${owners[@]}" | sort -u | tr '\n' ',' | sed 's/,$//')
        fi
    fi
    
    if [ -n "$REVIEWERS" ]; then
        echo "   추천 리뷰어: $REVIEWERS"
    else
        echo "   CODEOWNERS에서 리뷰어를 찾을 수 없습니다"
    fi
    
    echo ""
    echo -n "리뷰어를 수정하시겠습니까? [y/n]: "
    read -r modify_reviewers
    
    if [ "$modify_reviewers" = "y" ]; then
        echo -n "리뷰어 입력 (쉼표로 구분): "
        read -r manual_reviewers
        
        if [ -n "$manual_reviewers" ]; then
            REVIEWERS="$manual_reviewers"
        fi
    fi
}

infer_labels() {
    echo ""
    echo "🏷️  라벨 추론 중..."
    
    LABELS=""
    
    if [[ $CURRENT_BRANCH == feature/* ]] || echo "$COMMITS" | grep -qi "feat"; then
        LABELS="${LABELS},enhancement"
    fi
    
    if [[ $CURRENT_BRANCH == hotfix/* ]] || echo "$COMMITS" | grep -qi "fix"; then
        LABELS="${LABELS},bug"
    fi
    
    if echo "$COMMITS" | grep -qi "docs"; then
        LABELS="${LABELS},documentation"
    fi
    
    if echo "$COMMITS" | grep -qi "test"; then
        LABELS="${LABELS},testing"
    fi
    
    if echo "$COMMITS" | grep -qi "refactor"; then
        LABELS="${LABELS},refactoring"
    fi
    
    LABELS=$(echo "$LABELS" | sed 's/^,//')
    
    if [ -n "$LABELS" ]; then
        echo "   추론된 라벨: $LABELS"
    fi
}

select_pr_options() {
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "PR 생성 옵션"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "1) Draft PR로 생성 (기본)"
    echo "2) 즉시 리뷰 요청"
    echo "3) Auto-merge 활성화"
    echo ""
    echo -n "선택 [1/2/3, 기본=1]: "
    read -r pr_option
    
    PR_FLAGS=""
    AUTO_MERGE=false
    
    case "$pr_option" in
        2) PR_FLAGS="--web" ;;
        3)
            PR_FLAGS="--web"
            AUTO_MERGE=true
            ;;
        *) PR_FLAGS="--draft" ;;
    esac
}

create_pull_request() {
    echo ""
    echo "🚀 PR 생성 중..."
    
    local create_cmd="gh pr create --title \"$PR_TITLE\" --body \"$FINAL_PR_BODY\" --base \"$BASE_BRANCH\" --head \"$CURRENT_BRANCH\" $PR_FLAGS"
    
    if [ -n "$REVIEWERS" ]; then
        create_cmd="$create_cmd --reviewer \"$REVIEWERS\""
    fi
    
    if [ -n "$LABELS" ]; then
        create_cmd="$create_cmd --label \"$LABELS\""
    fi
    
    if eval "$create_cmd"; then
        echo ""
        echo "✅ PR 생성 완료!"
        
        PR_URL=$(gh pr view --json url -q .url)
        echo "   URL: $PR_URL"
        
        if [ "$AUTO_MERGE" = true ]; then
            echo ""
            echo "🔄 Auto-merge 활성화 중..."
            
            echo "머지 방법 선택:"
            echo "  1) merge (머지 커밋)"
            echo "  2) squash (스쿼시 머지, 기본)"
            echo "  3) rebase (리베이스)"
            echo -n "선택 [1/2/3]: "
            read -r merge_method
            
            case "$merge_method" in
                1) METHOD="merge" ;;
                3) METHOD="rebase" ;;
                *) METHOD="squash" ;;
            esac
            
            gh pr merge --auto --$METHOD
            echo "   ✅ Auto-merge 활성화 ($METHOD)"
        fi
        
        if command -v codex &> /dev/null; then
            echo ""
            echo -n "Codex 셀프 리뷰를 실행하시겠습니까? [y/n]: "
            read -r run_codex
            
            if [ "$run_codex" = "y" ]; then
                echo ""
                echo "🤖 Codex 리뷰 시작..."
                
                if codex review --pr "$PR_URL" --effort high; then
                    echo "   ✅ Codex 리뷰 완료"
                else
                    echo "   ⚠️  Codex 리뷰 실패"
                fi
            fi
        fi
        
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ PR 워크플로우 완료!"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "다음 단계:"
        echo "  1) PR 확인: $PR_URL"
        echo "  2) CI 상태: gh pr checks"
        echo "  3) 리뷰 대기 또는 추가 커밋"
        echo ""
    else
        echo ""
        echo "❌ PR 생성 실패"
        echo ""
        echo "가능한 원인:"
        echo "  1. 원격 브랜치 없음 (먼저 push 필요)"
        echo "  2. 이미 PR 존재"
        echo "  3. 권한 부족"
        echo "  4. Base 브랜치 잘못 지정"
        exit 1
    fi
}

#━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 메인 실행
#━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

main() {
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "🚀 Pull Request 자동 생성"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    check_environment
    setup_branches
    analyze_commits
    generate_pr_title
    generate_pr_body
    assign_reviewers
    infer_labels
    select_pr_options
    create_pull_request
}

main
```
{% endraw %}

---

## 실전 예시

### 예시 1: 기본 워크플로우

```console
$ claude
> /pr

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 Pull Request 자동 생성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 환경 확인 중...
✅ 환경 확인 완료

📂 브랜치 설정 중...
   현재 브랜치: feature/user-profile
   기본 브랜치: main

Base 브랜치를 선택하세요:
  1) main (기본)
  2) develop
  3) 직접 입력

선택 [1/2/3, 기본=1]: 1
   → Base: main

📊 커밋 분석 중...

포함될 커밋:
─────────────────────────────────────
8f4d1e5 docs(api): update auth endpoints
7a3b9c2 test(auth): add JWT refresh tests
6c2a8f1 feat(auth): add JWT refresh token
─────────────────────────────────────
커밋 수: 3
```

실행 결과:

```console
✅ PR 생성 완료!
   URL: https://github.com/user/repo/pull/456

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ PR 워크플로우 완료!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 단계:
  1) PR 확인: https://github.com/user/repo/pull/456
  2) CI 상태: gh pr checks
  3) 리뷰 대기 또는 추가 커밋
```

### 예시 2: Auto-merge 활성화

```console
$ bash ~/.claude/commands/pr.md

선택 [1/2/3, 기본=1]: 3

🚀 PR 생성 중...

✅ PR 생성 완료!
   URL: https://github.com/user/repo/pull/457

🔄 Auto-merge 활성화 중...
머지 방법 선택:
  1) merge (머지 커밋)
  2) squash (스쿼시 머지, 기본)
  3) rebase (리베이스)
선택 [1/2/3]: 2
   ✅ Auto-merge 활성화 (squash)
```

---

## 주요 기능

- ✅ 환경 자동 확인 (gh, git, 인증)
- ✅ Base 브랜치 선택 (main/master/develop)
- ✅ 커밋 분석 및 PR 제목 자동 생성
- ✅ AI를 활용한 PR 본문 작성 (Claude CLI)
- ✅ CODEOWNERS 기반 리뷰어 자동 지정
- ✅ 브랜치/커밋 기반 라벨 추론
- ✅ Draft/Web/Auto-merge 옵션
- ✅ Codex 셀프 리뷰 통합

## 필요한 도구

### 필수
- **GitHub CLI** (`gh`) - [[설치 가이드](https://cli.github.com/)](https://cli.github.com)
- **Git**

### 선택
- **Claude CLI** - PR 본문 AI 생성용
- **Codex** - 자동 코드 리뷰용

## 문제 해결

### "원격 브랜치 없음" 에러

```bash
# 먼저 브랜치를 push 하세요
git push -u origin <브랜치명>
```

### "이미 PR 존재" 에러

```bash
# 기존 PR 확인
gh pr list

# 기존 PR 닫기 (필요시)
gh pr close <PR번호>
```

### Claude CLI 없음 경고

AI 본문 생성이 스킵되고 템플릿이 사용됩니다. Claude CLI를 설치하면 AI 생성 기능을 사용할 수 있습니다.

---

## 팁

1. **PR 템플릿 사용**: `.github/PULL_REQUEST_TEMPLATE.md` 파일 생성
2. **CODEOWNERS 설정**: `.github/CODEOWNERS` 파일로 자동 리뷰어 지정
3. **커밋 컨벤션**: `feat:`, `fix:`, `docs:` 등의 접두사로 자동 라벨링
4. **빠른 실행**: `alias pr='bash ~/.claude/commands/pr.md'`를 `.bashrc`에 추가